<style>
  /* Field Ops: single-step visibility — overrides shared mobile media query */
  .step-section { display: none !important; width: 100%; justify-content: center; gap: 16px; align-items: flex-start; }
  .step-section.active { display: flex !important; }
  .step-section.phone-layout { flex-wrap: wrap; }
  .step-section.admin-layout { flex-direction: column; align-items: center; }
</style>
{{!-- Field Ops & Expense Management — 15 Steps --}}
      {{> step1-field-ops}}
      {{> step2-field-ops}}
      {{> step3-field-ops}}
      {{> step4-field-ops}}
      {{> step5-field-ops}}
      {{> step6-field-ops}}
      {{> step7-field-ops}}
      {{> step8-field-ops}}
      {{> step9-field-ops}}
      {{> step10-field-ops}}
      {{> step11-field-ops}}
      {{> step12-field-ops}}
      {{> step13-field-ops}}
      {{> step14-field-ops}}
      {{> step15-field-ops}}

